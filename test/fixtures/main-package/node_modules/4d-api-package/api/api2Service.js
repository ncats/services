var successResponse = function (req, res) {
    res.sendStatus(200);
};

exports.Routes = [
    // Test multiple http methods for the same route config
    { path: '/list/:listName/items/:id', httpMethod: 'GET', middleware: successResponse },
];
