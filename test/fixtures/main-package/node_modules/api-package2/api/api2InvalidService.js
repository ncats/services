var api2InvalidService = exports;

var routeHandler = function () {};

api2InvalidService.Routes = [

    // missing route path
    { httpMethod: 'POST', middleware: [routeHandler] },

    // invalid http method
    { path: '/list/:listName/items/:id', httpMethod: 'Invalid', middleware: [routeHandler] },

    // missing httpMethod
    { path: '/documents', middleware: [routeHandler] },

    // missing middleware
    { path: '/list/:listName/items/:id', httpMethod: 'DELETE' }
];